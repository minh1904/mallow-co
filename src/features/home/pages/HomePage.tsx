import { HeroSection, ProjectSection } from '../components';

const HomePage = () => {
  return (
    <div>
      <HeroSection />
      <ProjectSection />
    </div>
  );
};

export default HomePage;
